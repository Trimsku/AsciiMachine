/*#pragma once
#include <vector>
#include "GUI_o.hpp"
namespace ascii
{
    
    class Server {
        private:
            std::vector<GUI_o*> on_display_objects; // TODO: Create map x, y;
    };
} // namespace ascii */
